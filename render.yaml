# Render Blueprint: Web service serving pre-built gui-v2 static assets.
# Before first deploy: copy WASM into deploy/wasm/ and push (see docs/SETUP.txt section 5).
#
# https://render.com/docs/blueprint-spec
version: "1"
services:
  - type: web
    name: venus-gui-v2
    runtime: docker
    dockerfilePath: ./deploy/Dockerfile
    dockerContext: .
    plan: free
    region: oregon
