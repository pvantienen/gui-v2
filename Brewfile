# Local Mac only (Render does not use Homebrew). From this directory (gui-v2 repo root):
#   brew bundle install
# Then open Docker Desktop from Applications and wait until it says "Docker is running".
cask "docker"
